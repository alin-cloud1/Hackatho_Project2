# Copy this file to .env and fill in real values.
# Never commit the real .env file.

# Point the frontend at the backend API. Leave blank to run the frontend
# standalone against localStorage (no backend required).
VITE_API_URL=http://localhost:4000
